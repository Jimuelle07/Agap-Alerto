# Change Record — Agap Alerto

> Append-only log for changes to **Locked** docs. Selected because `time_budget_hours > 24`.

## Doc lifecycle status
All docs in this suite are currently **Draft** as of 2026-09-13 (first generation, phase-5.1/5.2/5.3
of this run). None have been marked **Locked** yet — that happens when the lead agrees a doc is
stable enough that further edits should be tracked as CRs rather than free revision. No CR exists
yet because no doc has reached that state.

The one revision made during this run (`seed/idea.md` F-001/§8, per `decision-ledger.md` §3) was to
the **seed**, not a generated `docs/` file, and predates phase-5.1 doc generation — it is recorded
in the Key's own `seed/idea.md` history and the decision ledger above, not as a CR.

## CR-### — none recorded yet
This file will gain entries once the lead locks a doc (typically at or after the first phase gate,
`implementation-plan.md` PH-01) and a subsequent edit is needed.
